int x=400;
int y=300;
void setup(){
  size(800,600);
}
void draw(){
  background(0);
  rect(x,y,40,40);
}
void keyPressed(){
  if (key == CODED) {
    if (keyCode == RIGHT) {
      x=x+50;
    } else if (keyCode == LEFT) {
      x=x-50;
    }
  }
}
